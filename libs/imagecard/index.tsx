import React from 'react';
import AppView from '../../common/AppView';
import {imageCardTypes} from '../../types/global';
const ImageCard: React.FC<Partial<imageCardTypes>> = () => {
  return <AppView></AppView>;
};

export default ImageCard;
